/**
 * Represents a string literal union of HTML element tags available in JSX.
 *
 * @description A valid HTML element tag name that can be used in JSX.
 */
export type HtmlElementTag = keyof JSX.IntrinsicElements;
