/**
 * Represents a type that can either be of the specified type or null.
 *
 * @template T - The base type for which a nullable version is created.
 * @description A type that can be either of the specified type or null.
 */
export type Nullable<T> = T | null;
