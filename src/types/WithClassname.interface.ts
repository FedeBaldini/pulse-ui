/**
 * Interface for components that accept a CSS class name.
 *
 * @property {string} className - CSS class name for styling the component.
 */
export interface WithClassname {
  className: string;
}
